<div class="flex items-center justify-between mb-4">
  <h1 class="text-2xl font-semibold">Submission #<%= @submission.id %> — <%= @form.title %></h1>
  <%= link_to 'All submissions', form_submissions_path(@form), class: "btn btn-ghost" %>
</div>

<div class="card"><div class="card-body">
  <dl class="divide-y divide-gray-200">
    <% @responses.each do |r| %>
      <div class="py-3 grid grid-cols-3 gap-4">
        <dt class="text-sm font-medium text-gray-600"><%= r.field.label %></dt>
        <dd class="col-span-2 text-sm text-gray-900 break-words"><%= r.value %></dd>
      </div>
    <% end %>
  </dl>
</div></div>
